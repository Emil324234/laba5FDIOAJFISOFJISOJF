<Window x:Class="practa5laba5.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:practa5laba5"
        Title="Login" Height="200" Width="300">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Label Content="Username:"/>
        <TextBox x:Name="usernameTextBox" Grid.Row="0"/>

        <Label Content="Password:" Grid.Row="1"/>
        <PasswordBox x:Name="passwordBox" Grid.Row="1"/>

        <Button x:Name="loginButton" Content="Login" Grid.Row="2" Click="LoginButton_Click"/>
    </Grid>
</Window>
